import { Tools } from '@/components/Tools'

export const metadata = {
  title: "Tools — BlueAgent",
  description: "31 security tools for autonomous AI agents on Base. Pay per call in USDC via x402 — no API keys.",
}

export default function ToolsPage() {
  return (
    <main>
      <Tools />
    </main>
  )
}
